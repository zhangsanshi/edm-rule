{
  "name": "edm-rule",
  "version": "0.1.1",
  "description": "edm规则,用于html-check解析",
  "main": "rule.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "image-size": "^0.5.0"
  }
}
